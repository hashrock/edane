# 現状の機能

- 基本的なツリーエディタ

# 公開前TODO（これをやったら公開できそう）

## 機能

- [ ] Markdownノードのレンダリング
- [x] Markdownとしてコピー（選択ノード以下の枝をMarkdownアウトラインとしてクリップボードへ）（#48）
- [x] D&Dでの画像アップロード（#50）

## 編集時の挙動

- [x] ノード削除時の挙動を枝ごと削除に変更（#42）
- [x] ESCで編集モードを抜けたとき、emptyノードを消す（#44）
- [x] 編集モード中はフォーカス色を変える（#45）
- [x] 編集モードで画像ノードに入るとURL入力欄に閉じ込められ、上下キーで隣のノードへ移動できない問題の修正（#46）

## ショートカットキー

- [x] 編集モード中でもショートカットで折りたたみできるようにする（⌘/Ctrl+.）（#47）
- [x] アウトラインモードを隠し機能的にショートカットキーで切り替えられるようにする（⌘/Ctrl+Shift+O）（#49）
- [ ] キーボードショートカットのカスタマイズ機能（ユーザーがキーバインドを変更できる）

## 見た目・ビジュアルノイズ低減

- [x] 公開ボタンの右三角アイコンが小さいのを直す（#40）
- [x] 保存ステータス：「未保存」は表示せず、「保存済み」はフェードインで表示する（#41）

# 公開後の検討課題（思想的なところ）

- Shiftで複数選択したい
  - 本当のニーズは「複数選択→一括削除」だと思われる

# 非機能要件

- [x] コピペの挿入位置をわかりやすくする（ペースト/子追加した新規ノードを破線アンバー枠で1.6秒ハイライト）
- [x] 文字選択と区別したノード選択の方法を用意する（既存のShift+矢印/ドラッグによるノード選択＝全体矩形ハイライトで充足。単一クリックで即編集の即時性は維持）
- [x] アンドゥ、リドゥの挙動を安定させる（Cmd+Shift+Zでredoが発火しないバグを修正＋Cmd+Y対応。node/browserテスト追加）
- [x] キーバインド操作の設計改善＋標準操作対応（散在していたキー処理を宣言的キーマップ`editorKeymap.ts`へ集約。選択モードの←/→で折りたたみ/展開・親子移動、Alt+↑↓で兄弟並べ替え、⌘/Ctrl+Bで太字、コマンドパレットを⌘/Ctrl+K（印刷と非衝突）へ変更。`?`とパレットからショートカット一覧を表示）
- [x] 大規模ツリーの編集パフォーマンス（ビューポートカリング導入。表示範囲＋余白に交差するノード/接続だけを再描画し、コストを総ノード数ではなく可視ノード数に依存させた。1000ノードで1操作あたり約114ms→約5msに改善。headlessブラウザ計測`MindmapEditor.edit-bench.browser.test.tsx`を追加）

# 新機能

- [x] R2への画像アップロード機能。ユーザごとに最大10MBの画像アップロードを可能にする。
- [x] 画像管理を可能にする。D1にも画像のメタデータを保存する。
- [x] リンクノードは、titleをfetchし、またfaviconを表示する
- [x] プロジェクト設定画面（/settings：画像管理・使用量・APIトークン）
- [x] 右クリックで、テキストノードの書式変更（フォントサイズ、太字など）
- [x] Markdownペースト機能。ペースト時にMarkdownを判定し、ダイアログで「分解してペースト（見出し/リストの階層をノード化）」「Markdownノードとしてペースト（新設のmarkdownノード）」「プレーンテキストとして貼り付け」から選択できる。

# むずそうな機能

- [x] カスタムカードを定義できる。スキーマを適用したカラムを持つカードを作ることができる。これにより、例えばWebページのFeaturesカードのような表現をすることができる。
  - スキーマレスの「オブジェクトカード」（`object`ノード）として実装。子ノードの `key: value` テキストがそのままフィールド行としてカード描画される（カード＝サブツリーのビュー。テキストに戻しても情報は失われない）。値は数値/URL/画像/日付/文字列を推論して描画し、数値はカンマ・通貨（¥）・パーセント・小数点桁数の表示形式を右クリックで指定できる。カード上でEnterすると直前カードのキーをプリフィルした兄弟カードを作成（schema by example）。
- [ ] 各ノードをJSONとしてWeb公開する機能（CORS対応）

# ユーザーフィードバック（「使ってみたメモ」より / 2026-08-01取得）

出典: https://edane.hashrock.info/notes/2a81898c-2d62-4604-8f78-41e9047e615d

## 直近

- [x] 旧デフォルト前提のテスト3件を設定軸で書き直す（#89）
- [ ] ←/→のデフォルトを `navigate` のままにするか決める（「←で選択が飛ぶ」の報告は navigate の仕様どおりの挙動。挙動は設定化済みで両方正解なので、初期値をどちらにするかの問題）
- [ ] キーボード不変条件を ←/→ にも広げる（CLAUDE.md と `keyboardEscape.browser.test.tsx` は現状 ↑/↓ のみ。arrowBehaviorの2値どちらでも成立する形で定義する）
- [x] `MindmapViewer.tsx` を捨て、読み取り専用モードの `MindmapEditor` に統一する（NoteEditor の readOnly prop として実装。dispatch 段階で編集を一括遮断し、保存系も全停止）（#93）

## 編集モードへの入り方

選択モード / 編集モードの二段構えは廃止しないと決定（「公開後の検討課題」の当該項目は取り下げ）。

- [ ] Enterで編集モードに入る（スペースだけだと慣れないという指摘）
- [x] ⌘/Ctrl+Enterで編集モードに入る（`editorKeymap.ts`の`sel-edit`にコード追加。Space/F2と同じく全選択で編集開始。素のEnter＝兄弟追加は`!mod(e)`で分離）
- [x] 選択済みノードの再クリックで、ダブルクリックを待たず編集モードに入る（mousedownで意図を記録し、ドラッグにならなかったmouseupで確定。カーソルはクリック位置。未選択ノードの1クリック目・ダブルクリック・ドラッグは従来どおり）

## 操作・表示

- [ ] 選択の上下移動を同階層優先にする（C→Bと動かしたいのに別階層のBへ飛ぶ）
- [ ] 左右キーで階層をまたいでノードを動かす（⌘+上下左右の要望はAlt+↑↓で充足済み）
- [ ] ドラッグ中に画面端へ来たら自動スクロール
- [ ] ドラッグ中のEscでキャンセル
- [ ] ノードの最大幅に上限を設ける
- [ ] 編集モード中のMarkdownペーストはダイアログを出さず、そのまま貼り付ける

## 公開まわり

- [ ] 公開ノートのリンクをコピーする動線（エディタと一覧 `app/pages/Notes/Index.tsx` のメニュー両方）
- [x] オーナー以外が `/notes/:id/edit` を開いたら `/notes/:id` へリダイレクト（公開ノートなら閲覧ページへ。非公開ノートはid存在を漏らさないため引き続き404、未ログインはログイン用にトップへ。判定は`app/utils/noteEditAccess.ts`に純関数として分離）

※ 方向キーでのノード移動 / ダブルクリックで編集・Escで抜ける / パンズームの滑らかさは「いいところ」として挙がっていた（リグレッション注意）

# 将来やる

- [ ] 複数ツリー対応（「Mapごとにツリーは1つずつ？」「ルートでClickDown→ドラッグしてしまう」はどちらもこの前提のずれ。後者は単独で直さない）
- [ ] 公開をノード単位にし、個別URLを発行する（現状の `isPublic` は表示可否・一覧バッジ・保存時の暗号化を兼ねている。noindexの要否もあわせて決める）
